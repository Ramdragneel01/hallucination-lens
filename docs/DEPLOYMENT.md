<!-- Generated by GitHub Copilot -->
# Deployment Guide

## Local Service Run

1. Install dependencies and package:
   - `pip install -r requirements.txt`
   - `pip install -e .`
2. Start API:
   - `uvicorn hallucination_lens.api:app --host 0.0.0.0 --port 8003`
3. Validate endpoints:
   - `/health`
   - `/score`
   - `/batch`
   - `/metrics`

## Docker Deployment

1. Build and run:
   - `docker compose up --build`
2. Validate:
   - API at `http://127.0.0.1:8003`
   - Prometheus at `http://127.0.0.1:9093`

## Environment Variables

1. `MODEL_NAME`
2. `FAITHFULNESS_THRESHOLD`
3. `MIN_THRESHOLD`
4. `MAX_THRESHOLD`
5. `MAX_BATCH_ITEMS`
6. `MAX_CONTEXT_CHARS`
7. `MAX_RESPONSE_CHARS`
8. `RATE_LIMIT_PER_MINUTE`
9. `HALLUCINATION_API_KEY` (optional; enables `X-API-Key` auth on scoring endpoints)
10. `CORS_ORIGINS`

## Probe and Endpoint Access

1. Keep `/health` unauthenticated for liveness and readiness probes.
2. When `HALLUCINATION_API_KEY` is set, require `X-API-Key` for `/score` and `/batch`.

## Release Pipelines

1. Package release artifacts generated by `.github/workflows/release.yml` on tag push.
2. Container image published to GHCR from the same release workflow.
